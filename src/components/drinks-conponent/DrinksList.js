import React from 'react';
import {View, Button, Text} from 'react-native';

const DrinksList = (drinksInfo) => {
    const {drinks, strDrink} = drinksInfo ;
    const info = {drinksInfo.strDrink}
  return (
    <View>
      <Text>{info}</Text>
    </View>
  );
};

export default DrinksList;
